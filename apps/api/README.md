# API de controle

Superfície reservada para os endpoints idempotentes de tarefas, planos, runs, aprovações e artefatos. A primeira implementação deve usar os schemas de `packages/contracts` e autenticar todas as operações antes de acessar o Supabase.
