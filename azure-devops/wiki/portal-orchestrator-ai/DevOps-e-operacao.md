# DevOps e operação

> Projeção operacional revisada em 2026-09-13. `docs/` permanece canônico; esta árvore acompanha a promoção para a `main`. Recursos planejados não equivalem a implantação.

Código canônico no GitHub; documentação em `docs/`; branches `dev` e `main`; produção publicada pela `main`. Esta árvore publica apenas o conteúdo do Orchestrator. O projeto Azure DevOps compartilhado é RPA Automatic e o planejamento deve usar o épico próprio.

Validações de mudanças relevantes: `npm test` e `npm run build`. A auditoria de ambientes é uma fotografia histórica; revalidar antes de qualquer deploy. Não usar o banco fiscal como ambiente do Orchestrator. A publicação desta Wiki não executa deploy nem corrige recursos remotos.
