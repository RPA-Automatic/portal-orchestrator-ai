# These are Edge Function secrets, not frontend variables.
# Configure through Supabase -> Edge Functions -> Secrets.
# Do not put actual values into committed files.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
